export type TCardDashboard = {
  title: string;
  description: string;
  buttonTo?: string;
  buttonText?: string;
  image: string;
  status: string;
  icon: string;
  date: string;
};

export type TcardSimple = {
  className: string;
  title: string;
  color: string;
  icon: string;
};
